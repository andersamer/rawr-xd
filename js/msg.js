'use strict';

class Msg {
    constructor(content, sender) {
        this.content = content;
        this.sender = sender;
        this.element = this.loadEl();
        this.display();
    }

    loadEl = () => {
        
    }
}